spring:
  application:
    name: user-center
  cloud:
    nacos:
      config:
        server-addr: ${NACOS_SERVER_ADDR:localhost:31446}
        name: user-center
        namespace: xxx
        file-extension: yml
      discovery:
        server-addr: ${NACOS_SERVER_ADDR:localhost:31446}