spring:
  application:
    name: stock-center
  cloud:
    nacos:
      config:
        server-addr: ${NACOS_SERVER_ADDR:localhost:31446}
        name: stock-center
        namespace: xxx
        file-extension: yml
      discovery:
        server-addr: ${NACOS_SERVER_ADDR:localhost:31446}