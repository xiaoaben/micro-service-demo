spring:
  application:
    name: order-center
  cloud:
    nacos:
      config:
        server-addr: ${NACOS_SERVER_ADDR:localhost:31446}
        name: order-center-conf
        namespace: xxx
        file-extension: yml
      discovery:
        server-addr: ${NACOS_SERVER_ADDR:localhost:31446}